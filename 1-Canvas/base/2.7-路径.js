
// context.beginPath(); // 将当前路径的所有子路径清除，重置当前路径
// context.closePath(); // 封闭路径，如封闭圆弧等
// context.rect(x, y, width, height); // 矩形子路径
// context.arc(x, y, radius, Math.PI / 180 * startAngle, Math.PI / 180 * endAngle, anticlockwise); // 圆弧子路径
// context.fill();

// 绘制不规则图形： 创建路径 + 对路径进行填充 => 比喻就是：使用隐形墨水来绘图

