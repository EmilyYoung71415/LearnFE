/**
 * @desc 数组操作练习
 *  
 *  console.table(thing)
 * 
 * 
 *  + filter 筛选出符合条件的数
 *  + map 把数组中的每个元素进行处理后，返回一个新的数组
 *  + filter 和 map 的结合使用
 *          .map(link => link.title)
            .filter(title => title.includes('CSS'));
            
    + reduce
        接受一个函数作为参数（这个函数可以理解成累加器），
        遍历数组的所有项，然后构建一个最终的返回值，这个值就是这个累加器的第一个参数

 *  +  
 */